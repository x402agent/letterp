# Approve Delegate

Authorize a delegate account to transfer up to a specified amount of tokens
on behalf of the account owner.

## Planned API
```rust
pub fn approve(
    source: &AccountInfo,
    delegate: &Pubkey,
    owner: &AccountInfo,
    amount: u64,
) -> ProgramResult
```

> 🚧 Coming Soon
