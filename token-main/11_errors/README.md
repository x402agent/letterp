# 11 — Errors

Custom error types for the pToken SDK. All errors are typed, descriptive,
and map cleanly to Solana's ProgramError system.

## Sub-modules
- `token_errors/` — Errors for core token operations
- `extension_errors/` — Errors specific to Token-2022 extensions

> 🚧 Coming Soon
