# Group Pointer Extension

Points a mint to a token group account that defines collection-level metadata
and membership rules for a set of related tokens.

## Key Parameters
- `authority` — Can update the group address
- `group_address` — The token group account

> 🚧 Coming Soon
